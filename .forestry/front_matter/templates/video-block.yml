---
label: "[Block] Video"
hide_body: true
fields:
- name: type
  type: text
  config:
    required: true
  label: Type
  hidden: true
  default: video_block
  description: Do not change. This field is only visible to Forestry admins.
- name: preview_image
  type: file
  config:
    maxSize: 64
  label: Preview Image
  description: Add a still image that will be a preview image for video
- name: url
  type: text
  config:
    required: false
  label: Video Link
  description: input video URL
- name: tint
  type: boolean
  label: Tint
  description: Toggle this on to add a darker blue overlay over preview image (this
    toggle will be green when the tint is enabled and grey when disabled).
- name: button
  type: field_group
  config: {}
  fields:
  - name: type
    type: text
    config:
      required: false
    hidden: true
    label: Type
    default: D
    description: Do not change. This field is only visible to Forestry admins.
  - name: action
    type: text
    config:
      required: false
    label: Action
    hidden: true
    default: video
    description: Do not change. This field is only visible to Forestry admins.
  - name: icon
    type: text
    config:
      required: false
    label: Icon
    hidden: true
    default: play
    description: Do not change. This field is only visible to Forestry admins.
  - name: text
    type: text
    config:
      required: false
    label: Text
    description: Do not change. This field is only visible to Forestry admins.
  - name: url
    type: text
    config:
      required: false
    label: URL
    description: Do not change. This field is only visible to Forestry admins.
  label: Button
- name: title
  type: text
  config:
    required: false
  label: Title
  description: Title of the video
- name: date
  type: datetime
  label: Date
  description: Optionally select a date that will be displayed above the title
  config:
    required: false
    date_format: 
    time_format: 
    display_utc: false
    export_format: YYYY-MM-DD
- name: tags
  type: tag_list
  default: []
  label: Tags
  description: Optionally enter tags that will be displayed above the title
- name: slate_video_type
  type: text
  config:
    required: false
  label: Slate Video Type
  hidden: true
  description: Do not change. This field is only visible to Forestry admins.
