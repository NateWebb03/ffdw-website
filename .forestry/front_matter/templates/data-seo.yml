---
label: "[Data] SEO"
hide_body: true
fields:
- name: seo
  type: field_group
  config: {}
  fields:
  - name: title
    type: text
    config:
      required: true
      max: 100
    label: Title
    description: The title for this web page
  - name: description
    type: text
    config:
      required: true
      max: 200
    label: Description
    description: The meta description for this page, which will appear in search engine
      results and social media previews. Each page can have a unique meta description.
      One to three sentences are recommended.
  label: SEO
  description: Populate the metadata associated with this content, like the page title
    and description.
