---
label: "[Comp] Header Variant 4"
hide_body: true
fields:
- name: header
  type: field_group
  config: {}
  fields:
  - name: type
    type: text
    config:
      required: false
    label: Type
    default: variant_4
  - name: heading
    type: text
    config:
      required: false
    label: Heading
    description: Enter text that will appear in the top left corner of the header
  - name: subheading
    type: text
    config:
      required: false
    label: Subheading
    description: Enter text that will appear in the bottom right corner of the header
  - name: image_1
    type: file
    config:
      maxSize: 0.75
    label: Image 1
    description: Add the image that will appear in the center of the header (landscape image is best)
  - name: sub_theme
    type: select
    default: red-green
    config:
      required: false
      options:
      - red-purple
      - red-green
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Sub Theme
    description: Select the gadient colors of the shapes in the header
  - name: nav_theme
    type: select
    default: dark
    config:
      required: false
      options:
      - dark
      - light
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Nav Theme
    description: Select the navigation text color from the dropdown menu - "dark" will make the text in the nav black | "light" will make the text in the nav white
  label: Header
  